---
alwaysApply: true
description: Boilerplate conventions, source-of-truth pointers, repo layout, and non-negotiable invariants.
---

# CLEAN ARCHITECTURE BOILERPLATE — CORE

**.NET backend boilerplate** with Clean Architecture, CQRS, and DDD. Local-only via .NET Aspire.

## SOURCE OF TRUTH (read before acting)

**Do not load overlapping skills when these docs cover the task.** Skills are for procedures not spelled out below.

| Document | When to consult |
|---|---|
| [`docs/CONSTITUTION.md`](docs/CONSTITUTION.md) | Non-negotiable invariants (architecture, CQRS, data, API, local run, naming) |
| [`docs/PRD.md`](docs/PRD.md) | Boilerplate scope and included patterns |
| [`docs/TECHNICAL.md`](docs/TECHNICAL.md) | Layer layout, CQRS pipeline, domain, persistence §6, API §7, testing §10 |
| [`docs/memory/current-status.md`](docs/memory/current-status.md) | Active work, blockers, session checklist |
| [`docs/memory/decisions.md`](docs/memory/decisions.md) | Accepted architecture decisions |
| [`docs/memory/known-issues.md`](docs/memory/known-issues.md) | Documented bugs and workarounds |

**Cross-reference:** `Constitution §N`, `PRD §N`, `Tech §N`.

When a **rule**, **skill**, or **doc** disagree, precedence is: **Constitution → PRD / TECHNICAL → scoped rule → skill**. Fix the lower layer in a follow-up PR.

## DOC ROUTING (prefer over skills)

| Work | Read first |
|------|------------|
| Architecture, layers, dependencies | Constitution I · Tech §1–3 |
| Domain / aggregates | Constitution I.2–3 · Tech §5 |
| CQRS, handlers, pipeline | Constitution II · Tech §4 |
| EF Core, schema, migrations | Constitution III · Tech §6 · `migration.mdc` |
| HTTP, errors, auth | Constitution IV · Tech §7 · `api-guidelines.mdc` |
| Aspire / local run | Constitution V · Tech §8–9 · `aspire.mdc` |
| Naming, file size, quality | Constitution VI |
| Tests | Constitution VII · Tech §10 · `backend-testing.mdc` |
| Frontend (web/) | `frontend.mdc`, `design-system.mdc` |

## NON-NEGOTIABLE INVARIANTS

Summarized here for quick scan; **Constitution is authoritative** if anything differs.

1. **Clean Architecture** — Domain → Application → Infrastructure; Api is the composition root.
2. **Domain is pure C#** — no EF Core, ASP.NET, MediatR, or Infrastructure in `Solution.Domain`.
3. **Aspire AppHost** is the local topology source of truth — no hand-authored `docker-compose.yml`.
4. **`Solution.ServiceDefaults` is mandatory** for Api.
5. **PostgreSQL is authoritative**; Redis holds rebuildable session cache only.
6. **Sample bounded context** — `User` aggregate; replace with your domain.

## REPOSITORY LAYOUT

```
src/       AppHost, ServiceDefaults, Api, Application, Domain, Infrastructure, Contracts
tests/     Domain.UnitTests, Api.IntegrationTests, Testing.Common  (see backend-testing.mdc)
web/       React 19 + Vite (not in .slnx; Yarn; run via Aspire `web` Vite app)
docs/      CONSTITUTION, PRD, TECHNICAL, memory/, specs/, plans/
.cursor/   rules/, skills/ (procedural playbooks only)
```

## PROCEDURAL SKILLS (when docs are not enough)

Read `.cursor/skills/<name>/SKILL.md` only for these workflows:

| Trigger | Skill |
|---------|--------|
| OpenAPI export / codegen / CI verify | `openapi-contract-sync` |
| Live DB inspection (read-only SQL via MCP) | `postgres-mcp` |
| Local env broken | `env-doctor` |
| Frontend server state | `tanstack-query` |
| shadcn / Tailwind UI | `shadcn`, `tailwind-patterns` |
| Commits / PRs / GitHub | `create-pr`, `git-commit-writer`, `pr-description-writer`, `github-cli` |
| Explicit code review request | `code-reviewer` |

## WHEN TO CONSULT WHICH RULE

| Concern | Rule |
|---|---|
| .NET services | `backend.mdc` |
| Tests in `tests/**` | `backend-testing.mdc` |
| React UI | `frontend.mdc` |
| HTTP / API contracts | `api-guidelines.mdc` |
| EF / PostgreSQL schema | `migration.mdc` |
| AppHost / local run | `aspire.mdc` |
| Specs / plans / changelog | `memory-artifacts.mdc` |

## OUT OF SCOPE (unless explicitly requested)

Message broker, transactional outbox, production CD, advanced observability, horizontal scaling.
